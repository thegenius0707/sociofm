# Machine-readable results

Large result files are excluded from Git. The Zenodo analysis record contains
the JSON outputs required to reproduce manuscript values. Place downloaded
files in this directory without changing their release filenames.
