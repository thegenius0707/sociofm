# Dataset manifests

The Zenodo dataset record includes monthly acquisition manifests, quality
summaries and SHA-256 checksums. Place the downloaded manifest bundle in this
directory. Raw GDELT cache archives are not required because each source URL is
stored in the monthly manifests.
